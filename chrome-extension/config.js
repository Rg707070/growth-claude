// ── GROWTH extension configuration ──────────────────────────────────────────
// Fill these in with your own project values before loading the extension.
// SUPABASE_URL / SUPABASE_ANON_KEY: same values as the GROWTH app's
//   NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY.
// APP_URL: the deployed GROWTH app URL (used for the "open app" link).
window.GROWTH_CONFIG = {
  SUPABASE_URL: 'https://YOUR-PROJECT.supabase.co',
  SUPABASE_ANON_KEY: 'YOUR-ANON-KEY',
  APP_URL: 'https://growth-claude.vercel.app',
}
